package com.example.myapplication3.ui.dashboard;

import android.os.Bundle;
import android.view.LayoutInflater;
import android.view.View;
import android.view.ViewGroup;

import androidx.annotation.NonNull;
import androidx.fragment.app.Fragment;

/**
 * Stubbed PostDetailFragment: functionality removed per request.
 * Returns an empty View to avoid layout/class reference errors.
 */
public class PostDetailFragment extends Fragment {

    public PostDetailFragment() {
    }

    @Override
    public View onCreateView(@NonNull LayoutInflater inflater,
                             ViewGroup container, Bundle savedInstanceState) {
        // Return an empty View; all UI logic removed.
        return new View(requireContext());
    }

}
